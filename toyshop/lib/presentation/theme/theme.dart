import "package:flutter/material.dart";

ThemeData lightMode = ThemeData(
  colorScheme: const ColorScheme.light(
      surface: Color(0xff074799),
      primary: Colors.black12,
      secondary: Colors.white,
      inversePrimary: Colors.white24,



));
