import "package:flutter/material.dart";

class AppColors {
  static const Color primary = Color(0xff074799);
  static const Color add = Color.fromARGB(255, 4, 187, 10);
  static const Color remove = Color.fromARGB(255, 255, 18, 1);
}
